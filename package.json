{
  "name": "stream-subscription-ui",
  "version": "0.0.1",
  "scripts": {
    "dev": "PROXY_URL='http://localhost:5000' PORT=4200 npm start",
    "start": "node server.js",
    "postinstall": "cd app && npm install -f --legacy-peer-deps"
  },
  "private": true,
  "dependencies": {
    "express": "4.17.1",
    "http-proxy-middleware": "^2.0.6"
  }
}
